import React from 'react';
import ReactDOM from 'react-dom';
import { LS } from '../../libraries/localStorage';

it('is already logged in localStorage', () => {
    if (LS.getToken(null))
document.createElement('div');
	ReactDOM({ LS.getToken }, div);
});
